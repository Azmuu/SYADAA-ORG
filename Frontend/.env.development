# Leave unset in dev → frontend calls same-origin `/api/*` and Vite proxies to Express (no CORS).
# Uncomment only if the API is on another host and you manage CORS there:
# VITE_API_URL=http://localhost:5001/api
